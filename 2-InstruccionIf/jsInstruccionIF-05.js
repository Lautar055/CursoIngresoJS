//Lautaro D'Andrea
function mostrar() {
  let edad = parseInt(document.getElementById("txtIdEdad").value);

  if (edad >= 18 || edad < 13) {
    alert("No sos adolescente");
  }
} //FIN DE LA FUNCIÓN
